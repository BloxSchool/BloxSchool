local function awardBadge(onPlayerJoin)
    print('Functions are a great way to trigger something, such as awarding badges (see badge.lua for documentation)')
    function
        warn("However, functions can cause issues if you don't know how to use them")
        